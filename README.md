## Running the Project

Previously this project included a `docker-compose.yml` for local setup.
It has now been removed from version control to keep environment-specific
configuration local.

To run the project:
1. Start each service individually using its Dockerfile, or
2. Create your own `docker-compose.yml`
